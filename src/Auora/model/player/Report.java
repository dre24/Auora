//Under development

package Auora.model.player;

import java.io.Serializable;

public class Report implements Serializable {

    //CLOSED OFF FOR TEMP.

    //public static Enem enem;

    public static void staffin(Player p) {
        //appendData("logs/report/" + player.getUsername() + ".txt", player.getUsername() + ": Reported ");
        //System.out.println("New log added by "+p.getUsername()+".");
    }

    public static void accounts(Player p) {
        //if(p.getRights555() < 2) {
        // appendData("logs/report/" + player.getUsername() + ".txt", player.getUsername() + ": Reported for account selling.");
        // }  else {
        // p.getFrames().sendChatMessage(0, "This feature is not available to player at this moment.");
        //}
    }
}